# Neon Postgres (Marketplace). Pull locally with: vercel env pull .env.local --yes
DATABASE_URL=

# Vercel Blob. Created with `vercel blob store add` (or Marketplace) and injected into the project.
BLOB_READ_WRITE_TOKEN=
